# No local overrides needed — in dev, open http://localhost:3002 (the router), not http://localhost:5173.
# The router proxies to Vite (DEV_VITE_URL) when no pod is running, and to the pod when running.
# Vite runs as a background HMR server only; its port (5173) is not used directly.
