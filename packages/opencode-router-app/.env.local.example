# Dev override: when the pod is running, redirect to the router directly instead of Vite's root.
# In production this is not needed — the router serves the SPA itself.
VITE_ROUTER_URL=http://localhost:3002
