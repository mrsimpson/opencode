// src/plugin.ts
var warnedMissingEnv = false;
async function pushEvent(event) {
  const routerUrl = process.env.OPENCODE_ROUTER_URL;
  const hash = process.env.OPENCODE_SESSION_HASH;
  const secret = process.env.OPENCODE_POD_SECRET;
  if (!routerUrl || !hash || !secret) {
    if (!warnedMissingEnv) {
      warnedMissingEnv = true;
      console.warn(
        "opencode-router-plugin: OPENCODE_ROUTER_URL, OPENCODE_SESSION_HASH, or OPENCODE_POD_SECRET is unset; pushEvent disabled."
      );
    }
    return;
  }
  try {
    const res = await fetch(`${routerUrl}/api/sessions/${hash}/progress`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "x-pod-secret": secret
      },
      body: JSON.stringify(event)
    });
    if (!res.ok) {
      console.warn(`opencode-router-plugin: pushEvent ${event.type} \u2192 HTTP ${res.status}`);
    }
  } catch (err) {
    console.warn(`opencode-router-plugin: pushEvent ${event.type} failed:`, err);
  }
}

// src/port-watcher.ts
var POLL_INTERVAL_MS = 5e3;
var MIN_PORT = 3e3;
var OPENCODE_PORT = 4096;
var PROC_NET_TCP = "/proc/net/tcp";
function parseProcNetTcp(content) {
  const ports = /* @__PURE__ */ new Set();
  for (const line of content.split("\n").slice(1)) {
    const trimmed = line.trim();
    if (!trimmed) continue;
    const hexPort = trimmed.split(/\s+/)[1]?.split(":")[1];
    if (!hexPort) continue;
    const port = parseInt(hexPort, 16);
    if (port > MIN_PORT && port !== OPENCODE_PORT && port <= 65535) {
      ports.add(port);
    }
  }
  return Array.from(ports).sort((a, b) => a - b);
}
function startPortWatcher() {
  const routerUrl = process.env.OPENCODE_ROUTER_URL;
  const hash = process.env.OPENCODE_SESSION_HASH;
  const secret = process.env.OPENCODE_POD_SECRET;
  if (!routerUrl || !hash || !secret) {
    return () => {
    };
  }
  if (process.platform !== "linux") {
    return () => {
    };
  }
  let stopped = false;
  let lastPushed = null;
  const poll = async () => {
    if (stopped) return;
    try {
      const content = await Bun.file(PROC_NET_TCP).text();
      const ports = parseProcNetTcp(content);
      const key = ports.join(",");
      if (key !== lastPushed) {
        try {
          const res = await fetch(`${routerUrl}/api/sessions/${hash}/ports`, {
            method: "POST",
            headers: {
              "Content-Type": "application/json",
              "x-pod-secret": secret
            },
            body: JSON.stringify({ ports })
          });
          if (!res.ok) {
            console.warn(`opencode-router-plugin: pushPorts \u2192 HTTP ${res.status}`);
          } else {
            lastPushed = key;
          }
        } catch (err) {
          console.warn("opencode-router-plugin: pushPorts failed:", err);
        }
      }
    } catch {
    }
    if (!stopped) setTimeout(() => void poll(), POLL_INTERVAL_MS);
  };
  const timer = setTimeout(() => void poll(), POLL_INTERVAL_MS);
  return () => {
    stopped = true;
    clearTimeout(timer);
  };
}

// src/index.ts
var messageRoles = /* @__PURE__ */ new Map();
var allowedSessionIds = null;
function isAllowed(sessionID) {
  return allowedSessionIds === null || allowedSessionIds.has(sessionID);
}
function lockToSession(sessionID) {
  if (allowedSessionIds === null) {
    allowedSessionIds = /* @__PURE__ */ new Set([sessionID]);
  } else {
    allowedSessionIds.add(sessionID);
  }
}
async function runStartupReplay(input) {
  try {
    const listResult = await input.client.session.list();
    const sessions = listResult.data ?? [];
    if (sessions.length > 0) {
      allowedSessionIds = new Set(sessions.map((s) => s.id));
    }
    for (const session of sessions) {
      if (session.title) {
        await pushEvent({ type: "session.title", sessionID: session.id, title: session.title });
      }
      const msgResult = await input.client.session.messages({ path: { id: session.id } });
      const messages = msgResult.data ?? [];
      for (const entry of messages) {
        const msg = entry.info;
        for (const part of entry.parts ?? []) {
          if (part.type === "text" && (msg.role === "user" || msg.role === "assistant")) {
            await pushEvent({
              type: msg.role === "user" ? "message.user" : "message.assistant",
              partID: part.id,
              messageID: msg.id,
              sessionID: session.id,
              text: part.text ?? "",
              time: msg.time?.created ?? Date.now()
            });
          }
        }
      }
    }
  } catch (err) {
    console.warn("opencode-router-plugin: startup replay failed:", err);
  }
}
var RouterPlugin = async (input) => {
  setTimeout(() => runStartupReplay(input), 5e3);
  startPortWatcher();
  return {
    event: async ({ event }) => {
      const e = event;
      if (e.type === "session.created" || e.type === "session.updated") {
        const title = e.properties?.info?.title;
        const sessionID = e.properties?.sessionID ?? e.properties?.info?.id;
        if (!sessionID) return;
        if (e.type === "session.created") lockToSession(sessionID);
        if (title && isAllowed(sessionID)) {
          await pushEvent({ type: "session.title", sessionID, title });
        }
      }
      if (e.type === "message.updated") {
        const info = e.properties?.info;
        if (info?.id && info?.role) messageRoles.set(info.id, info.role);
      }
      if (e.type === "message.part.updated") {
        const part = e.properties?.part;
        if (part?.type === "text") {
          const sessionID = part.sessionID ?? e.properties?.sessionID;
          const role = messageRoles.get(part.messageID);
          if (role === "user" && isAllowed(sessionID)) {
            await pushEvent({
              type: "message.user",
              partID: part.id,
              messageID: part.messageID,
              sessionID,
              text: part.text ?? "",
              time: e.properties?.time ?? Date.now()
            });
          }
        }
      }
    },
    "experimental.text.complete": async (inp, output) => {
      if (!isAllowed(inp.sessionID)) return;
      await pushEvent({
        type: "message.assistant",
        partID: inp.partID,
        messageID: inp.messageID,
        sessionID: inp.sessionID,
        text: output.text,
        time: Date.now()
      });
    }
  };
};
var index_default = { id: "opencode-router", server: RouterPlugin };
var __test = {
  reset() {
    allowedSessionIds = null;
    messageRoles.clear();
  },
  getAllowedSessionIds() {
    return allowedSessionIds;
  },
  isAllowed,
  lockToSession,
  runStartupReplay
};
export {
  __test,
  index_default as default
};
